# KILROY — reduce legacy cache layers (Field-direct gains)
# Page cache still exists for Linux ABI; Field Die sheds retention aggressively.
CONFIG_TRANSPARENT_HUGEPAGE=n
CONFIG_NUMA_BALANCING=n
# Smaller dentry/inode pressure — field paths prefer direct coherence
CONFIG_SLUB_TINY=y